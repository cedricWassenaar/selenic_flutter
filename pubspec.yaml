name: selenic_design
description: Selenic Design System for Flutter. A set of coherent, themable, and extensible widgets following the Selenic Design System. Based on Selenic Design System.
version: 1.1.0
# homepage:
repository: https://github.com/cedricWassenaar/selenic_flutter

environment:
  sdk: ">=3.3.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  moon_icons: ^1.1.0
  moon_tokens: ^0.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  lint: ^2.1.2

flutter:
  uses-material-design: true
